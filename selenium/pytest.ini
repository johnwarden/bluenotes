[pytest]
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts = -ra --tb=short
markers =
    live: hits BASE_URL (local webpack or https://bluenotes.social)
    write: optional propose/vote; skipped unless credentials are set
